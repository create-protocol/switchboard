[profile.default]
src = "contracts"
test = "tests-foundry"
out = "out"
libs = ["lib"]
solc = "0.8.20"
optimizer = true
optimizer_runs = 200
verbosity = 2

# Remappings (forge-std default path)
remappings = [
    "forge-std/=lib/forge-std/src/",
]
